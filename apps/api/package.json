{
  "name": "api",
  "private": true,
  "version": "0.0.0",
  "type": "module",
  "description": "A backend for handling video textures, mediating between rivvon and slyce.",
  "main": "src/index.ts",
  "scripts": {
    "dev": "wrangler dev",
    "build": "wrangler deploy --dry-run",
    "deploy": "wrangler deploy",
    "db:migrations:create": "wrangler d1 migrations create rivvon-textures",
    "db:migrations:list:local": "wrangler d1 migrations list rivvon-textures --local",
    "db:migrations:list:remote": "wrangler d1 migrations list rivvon-textures --remote",
    "db:migrations:apply:local": "wrangler d1 migrations apply rivvon-textures --local",
    "db:migrations:apply:remote": "wrangler d1 migrations apply rivvon-textures --remote",
    "test": "vitest",
    "test:run": "vitest run",
    "lint": "echo 'No linter configured'"
  },
  "dependencies": {
    "aws4fetch": "^1.0.20",
    "hono": "^4.11.7",
    "nanoid": "^5.1.6"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "^4.20260131.0",
    "typescript": "^5.9.3",
    "vitest": "^4.1.7",
    "wrangler": "^4.61.1"
  }
}
